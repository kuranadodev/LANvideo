import { useEffect, useRef } from 'react';
export function WaveformCanvas({ values }: { values: number[] }) { const ref = useRef<HTMLCanvasElement>(null); useEffect(() => { const c = ref.current; if (!c) return; c.width = c.clientWidth; c.height = c.clientHeight; const ctx = c.getContext('2d')!; ctx.clearRect(0,0,c.width,c.height); ctx.strokeStyle='#4cc9f0'; ctx.beginPath(); values.forEach((v,i)=>{ const x=i/Math.max(1,values.length-1)*c.width; const y=c.height/2-v*c.height*0.45; i?ctx.lineTo(x,y):ctx.moveTo(x,y); }); ctx.stroke(); }, [values]); return <canvas className="plot" ref={ref}/>; }
