export function MetricsPanel({ status, metrics, audio }: { status: any; metrics: any; audio: any }) { return <section className="card"><h2>状态/性能</h2><p>管线状态：{status?.state ?? '未知'}</p><p>视频：{status?.video?.running ? '运行中' : '停止'} {status?.video?.error && <span className="error">{status.video.error}</span>}</p><p>音频：{status?.audio?.running ? '运行中' : '停止'} {status?.audio?.error && <span className="error">{status.audio.error}</span>}</p><p>FPS：{metrics?.fps?.toFixed?.(1) ?? '-'}</p><p>算法耗时：{metrics?.algorithm_ms?.toFixed?.(1) ?? '-'} ms</p><p>RMS：{audio?.rms?.toFixed?.(4) ?? '-'}</p><p>Peak：{audio?.peak?.toFixed?.(4) ?? '-'}</p><p>CPU：{status?.system?.cpu_percent ?? '-'}%</p><p>内存：{status?.system?.memory_percent ?? '-'}%</p></section>; }
