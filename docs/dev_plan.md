# 开发计划

第一版目标：在 Debian 13 小主机上通过浏览器访问 Dashboard，启动 USB 摄像头视频处理、MediaMTX WebRTC 预览、麦克风波形和频谱可视化。

关键决策：当前仓库即项目根目录；不使用 mock；支持网页手选设备；启动管线时同时尝试视频和音频；音频失败不影响视频；第一版用 iframe 播放 MediaMTX；参数保存后由用户确认重启生效；UI 中文；不做认证。
